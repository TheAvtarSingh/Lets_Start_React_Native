import { StyleSheet } from "react-native";

// Internal Styling
const styles  = StyleSheet.create({
    textBox:{
      color:'white',
      fontSize: 25,
      backgroundColor:"red",
      borderColor:"Black",
      borderWidth:2,
      width:100,
      borderRadius:10,
    }
  })

  export default styles;